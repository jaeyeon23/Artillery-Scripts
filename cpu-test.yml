config:
  target: "http://localhost/"
  phases:
    - duration: 60        # 초
      arrivalRate: 4      # 매 초 생성할 사용자 수
      name: Warm up
scenarios:
  # We define one scenario:
  - name: "just get hash"
    flow:
      - get:
          url: "/hash/123"